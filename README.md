# Cyrus-Capital-admin (merged into Cyrus-Capital)

The admin panel now lives inside the main `Cyrus-Capital` repo, at `/admin`
(deployed at `/admin` and `/admin/support`). This repo is no longer used —
keep deploying from `Cyrus-Capital` going forward.
